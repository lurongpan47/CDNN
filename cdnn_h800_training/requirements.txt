# =============================================================================
# CD-Transformer H800 Training — Python Dependencies (Hopper SM90, FP8)
# =============================================================================
# Tested with Python 3.10-3.12 on NVIDIA H800 (CUDA 12.x required for FP8).
#
# Install:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# =============================================================================

# --- Core (required) ---
# PyTorch 2.1+ is required for:
#   - torch._scaled_mm  (native FP8 GEMM on Hopper Tensor Cores)
#   - F.scaled_dot_product_attention (Flash Attention v2)
#   - FSDP with use_orig_params  /  fused AdamW
torch>=2.1.0
numpy>=1.23.0

# --- Data preparation (OPTIONAL) ---
# Only needed for non-byte tokenizers in prepare_data.py.
#   - Nothing here is required if you use:  --tokenizer byte   (fully offline)
#     or  --source synthetic  (no tokenizer at all).
#   - modelscope: DeepSeek/Qwen tokenizers from ModelScope (China-friendly).
#   - transformers: required alongside modelscope; also for HuggingFace ids
#     (prepare_data.py defaults HF_ENDPOINT to https://hf-mirror.com).
#   - tiktoken: GPT-2/GPT-4 BPE.
modelscope>=1.9.0
transformers>=4.30.0
tiktoken>=0.4.0
datasets>=2.10.0

# --- Utilities ---
tqdm>=4.65.0
